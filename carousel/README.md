# carousel

`index.php` reads a local JSON definition and calls `modala` against `document.body`. The three image files are the carousel's local media. It is a compact example of JSON-defined UI plus local assets; inspect the JSON payload returned by the PHP entry point before extending it.
