# login

`index.html` mounts `<login>` with `login-page="../login/login.html"` and `registration-page="../login/registration.html"`. The two HTML files are local form-page sources. The component builds the tabs/forms and uses the configured pages for its AJAX flow; it is a UI example, not a user database or password service.
