# XI XI demo fragment

`index.php` returns the replaceable HTML fragment consumed by the demo's `xi` timer. The browser swaps the target content and dispatches `domPipe:listen`; it does not reload the whole page.
