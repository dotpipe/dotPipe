# XI XI demo API

`index.php` is the JSON polling endpoint used by the demo page. It is intentionally small so the page can show changing API data and normalized telemetry. Route it through the ELSN gateway when testing pause/deny behavior.
