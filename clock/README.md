# clock

`index.html` creates a JSON-driven output/control example in `#pageRoot` and uses the runtime's `modala` function for embedded definitions. `outputs.css` contains the dial/output styling. The file also contains a commented simplified renderer for reference; the active page loads `../domPipe.js`.
