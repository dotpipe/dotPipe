# ELSN health endpoint

`index.php` is the smallest endpoint-directory example. The ELSN gateway should expose it as `/health`; the endpoint itself is only the child application and does not perform allow/deny or pause checks.
