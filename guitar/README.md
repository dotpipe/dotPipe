# guitar

`index.php` disables caching, returns `index.json`, and includes `domPipe.js`. The JSON and PHP files describe the page/content; the PNG, GIF, WEBP, and JPEG files are local media; `styles.css` and `dark-theme.css` are the presentation layers. This is a media-rich JSON page example, not an image-upload service.
