# API

`index.php` emits/loads the `arrow-area.json` example through `<pipe id="arrow-area-pipe" ajax="arrow-area.json" class="strict-json">`. The directory demonstrates a JSON endpoint and payload pair; it is suitable for testing relative `ajax`, strict JSON, and target insertion.
