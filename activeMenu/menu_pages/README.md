# activeMenu/menu_pages

`collective.php` returns the available page definitions as JSON/PHP output. `arrow-area.json` is a small static component payload. These files are consumed by the active-menu shell; they are data endpoints, not standalone pages.
