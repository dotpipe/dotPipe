# landing

`index.html` is the broad domPipe showcase: `modala`, basic and multi-part AJAX markup, JSON modal content, carousel markup, and combined examples. It includes the runtime directly. Several URLs are intentionally illustrative (`hello-world.php`, `part1.json`, and similar); verify or supply those resources before treating every showcase button as a complete application.
