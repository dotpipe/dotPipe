# inline

`index.html` is the inline macro playground. Its visible demos use IDs and `inline` values for increment/decrement, boolean toggle, clamping, and sleep/asynchronous behavior. `data.json` is a small local payload. Load it through a PHP server so `../domPipe.js` resolves correctly.
