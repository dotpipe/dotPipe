# csv-test

`products.html` is the consumer page for the domPipe CSV component. `sales.csv` is the local source. The page's controls exercise CSV loading and the table/list/card presentation behavior implemented in `domPipe.js`; it is a browser example and has no database connection.
